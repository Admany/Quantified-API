{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "${version}",
  "name": "${mod_name}",
  "description": "${mod_description}",
  "authors": [
    "${mod_authors}"
  ],
  "license": "${mod_license}",
  "icon": "quantifiedapi.png",
  "environment": "*",
  "entrypoints": {
    "main": [
      "org.admany.quantified.core.fabric.wide.QuantifiedCoreFabricWide"
    ],
    "client": [
      "org.admany.quantified.core.fabric.wide.QuantifiedCoreFabricClientWide"
    ]
  },
  "depends": {
    "fabricloader": ">=${fabric_loader_version}",
    "minecraft": "${minecraft_version_range}",
    "fabric-api": "*"
  }
}
